# Contributor: beingmeta repository manager <repoman@beingmeta.com>
# Maintainer: beingmeta repository manager <repoman@beingmeta.com>
pkgname=libu8
pkgver=2.6.9
pkgrel=@U8PATCHLEVEL@
pkgdesc="UTF-8 string and comptability library"
url="https://libu8.org/"
arch="all"
license="LGPL"
depends="ldns libressl libuuid"
makedepends="ldns-dev libressl-dev libuuid libc-dev alpine-sdk"
install=""
#subpackages="$pkgname-dev $pkgname-doc"
subpackages=""
source="https://github.com/beingmeta/libu8/archive/@GITBRANCH@.tar.gz"
builddir="libu8-@GITBRANCH@"

build() {
	pwd
	./configure \
		--build=$CBUILD \
		--host=$CHOST \
		--prefix=/usr \
		--sysconfdir=/etc \
		--mandir=/usr/share/man \
		--localstatedir=/var \
		--without-sudo
	make
}

check() {
	cd src/${builddir}
	pwd
	make
}

package() {
	cd src/${builddir}
	make DESTDIR="$pkgdir" SUDO= install
}
